// Gemini service removed to convert the app into a pure static landing page.
export const refineContent = async (currentText: string, _instruction: string): Promise<string> => {
  return currentText;
};
